{
  "name": "yapcad-web-viewer",
  "version": "0.1.0",
  "description": "Web-based viewer for yapCAD geometry (Phase 1)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "serve": "npx serve public -p 3000"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
